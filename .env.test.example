# Copy this to .env.test and fill in real credentials.
# .env.test is gitignored — never commit credentials.
#
# Use a dedicated test Supabase account (not your main admin account)
# so tests can safely create and delete Airtable records.

TEST_EMAIL=your-test-account@email.com
TEST_PASSWORD=your-test-password
